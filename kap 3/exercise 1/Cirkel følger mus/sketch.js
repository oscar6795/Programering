 function setup() {
  createCanvas(1920,1080);
  strokeWeight (50);
}

function draw() {
  background (240);

if (mouseIsPressed == true) {
  if(mouseButton == LEFT) {
   ellipse (mouseX, mouseY, mouseX, mouseY)
  
}
}
}